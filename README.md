# rabbit_quest

## 🎮 Обзор проекта
**Rabbit Quest — интерактивная веб-игра-квест**, разработанная на стеке `Node.js + Express.js`. Проект демонстрирует навыки полного цикла разработки: от архитектуры приложения и *CI/CD* до развертывания в *Docker-контейнерах*.

## 🎯 Цели проекта
- Создание увлекательного **веб-квеста** с интерактивными элементами
- Освоение полного стека разработки `(frontend + backend)`
- Автоматизация `CI/CD` процессов
- Развертывание в `production-ready` окружении с `Docker`

### 🛠️ Технологический стек
```
Frontend: HTML5, CSS3, Vanilla JavaScript
Backend: Node.js 18+, Express.js
CI/CD: GitHub Actions + Docker
Deployment: Docker containers
Monitoring: Docker logs + Express logging (morgan)
```

## 🚀 Быстрый старт

1. Клонирование и установка

```
git clone https://github.com/Cricko7/rabbit_quest.git
cd rabbit_quest
npm install
```

2. Локальный запуск

```
# Разработка
npm start

# Production
NODE_ENV=production npm start

```
> Сервер запустится на http://localhost:3000


3. Docker (рекомендуется)

```
# Сборка образа
docker build -t rabbit-quest .

# Запуск контейнера
docker run -d --name rabbit-quest -p 3000:3000 rabbit-quest
```

### 📁 Структура проекта

```
rabbit_quest/
├── app.js              # Основное Express приложение
├── package.json        # Зависимости и скрипты
├── Dockerfile         # Docker-конфигурация
├── .github/
│   └── workflows/     # GitHub Actions CI/CD
│       └── deploy.yml
├── static/            # Статические файлы (CSS, JS, изображения)
├── logs/              # Логи (монтируется в Docker)
└── README.md          # Эта документация
```

### 🔄 CI/CD Автоматизация
#### Проект полностью автоматизирован через GitHub Actions:
1. Push в main → Сборка Docker-образа
2. Push образа в Docker Hub
3. Автоматический деплой на production-сервер через SSH
4. Zero-downtime обновление контейнера

### Workflow схема:

```
GitHub → Docker Build → Docker Hub → SSH Deploy → Docker Run
```

### 🐳 Docker Deployment
#### Запуск в production
```
docker run -d \
  --name rabbit-quest-prod \
  --restart unless-stopped \
  -p 3000:3000 \
  -v /host/logs:/usr/src/app/logs \
  cr1cke77e/rabbitquest:latest
```

#### Управление контейнером
```
# Логи в реальном времени
docker logs -f rabbit-quest-prod

# Перезапуск
docker restart rabbit-quest-prod

# Обновление
docker pull cr1cke77e/rabbitquest:latest && docker restart rabbit-quest-prod
```

### 📊 Логирование
#### Express Logging (Morgan)

```
app.use(morgan('combined', { stream: accessLogStream }));
```

#### Docker Logs

```
# Все логи приложения
docker logs rabbit-quest-prod

# Следить за логами в реальном времени
docker logs -f rabbit-quest-prod

# Последние N строк
docker logs --tail 100 rabbit-quest-prod
```

### 🔧 Конфигурация
#### Переменные окружения

```
NODE_ENV=production          # production/development
PORT=3000                    # Порт сервера
LOG_LEVEL=info               # Уровень логирования
```

__________

## 🎯 Функциональность квеста
- Интерактивный сюжет с ветвлением
- Система состояний пользователя
- Валидация действий на сервере
- Сохранение прогресса сессии
- Адаптивный интерфейс

________
### 🤝 Контрибьютинг
- Форкните репозиторий
- Создайте ветку feature/ваша-фича
- Коммитьте с понятными сообщениями
- Push в ветку и создайте Pull Request

### 📄 Лицензия
#### MIT License — используйте свободно!

##### Автор: Степанов Роман